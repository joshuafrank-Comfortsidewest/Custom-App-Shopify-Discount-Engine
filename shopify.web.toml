name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "node scripts/prisma-generate-safe.mjs"
dev = "node scripts/prisma-migrate-safe.mjs && npm exec react-router dev"
